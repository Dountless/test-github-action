name: Test Pipeline

on:
  push:
   branches: 
     - release/*

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Build successful"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: stg

    steps:
      - name: Deploy
        run: |
          echo "Deploying to STG environment"
          echo "Environment approval and secrets are now active"
